import { databaseRequest } from "@/lib/supabase/server";
export type CustomerRow={id:string;full_name:string;email:string;phone:string|null;account_status:string;service_addresses:{id:string;line1:string;city:string;region:string;postal_code:string;preferred_return_location:string|null;access_instructions:string|null;gate_information:string|null;animal_warning:string|null;municipalities:{name:string}|null;trash_pickup_schedules:{weekday:number|null;source:string;verification_status:string;cleaning_day_assignments:{normal_weekday:number;cleaning_date:string|null}[]}[]}[];subscriptions:{payment_status:string;subscription_status:string;service_status:string;service_plan_versions:{base_price_cents:number|null;service_plans:{id:string;display_name:string}}}[];bins?:never};
export async function portalCustomer(){const rows=await databaseRequest<CustomerRow[]>("customers?select=id,full_name,email,phone,account_status,service_addresses(id,line1,city,region,postal_code,preferred_return_location,access_instructions,gate_information,animal_warning,municipalities(name),trash_pickup_schedules(weekday,source,verification_status,cleaning_day_assignments(normal_weekday,cleaning_date))),subscriptions(payment_status,subscription_status,service_status,service_plan_versions(base_price_cents,service_plans(id,display_name)))&limit=1");if(!rows[0])throw new Error("No customer is linked to this test identity");return rows[0];}
export type CrmCustomer={id:string;full_name:string;account_status:string;service_addresses:{municipalities:{name:string}|null;trash_pickup_schedules:{weekday:number|null;cleaning_day_assignments:{normal_weekday:number}[]}[]}[];subscriptions:{service_plan_versions:{service_plans:{id:string;display_name:string}}}[]};
export async function crmCustomers(filters:{q?:string;plan?:string;status?:string;municipality?:string;pickup?:string}){const params=new URLSearchParams({select:"id,full_name,account_status,service_addresses(municipalities(name),trash_pickup_schedules(weekday,cleaning_day_assignments(normal_weekday))),subscriptions(service_plan_versions(service_plans(id,display_name)))",order:"full_name"});if(filters.q)params.set("full_name",`ilike.*${filters.q.replace(/[%*,()]/g,"")}*`);if(filters.status)params.set("account_status",`eq.${filters.status}`);const rows=await databaseRequest<CrmCustomer[]>(`customers?${params}`);return rows.filter(c=>(!filters.plan||c.subscriptions.some(s=>s.service_plan_versions.service_plans.id===filters.plan))&&(!filters.municipality||c.service_addresses.some(a=>a.municipalities?.name===filters.municipality))&&(!filters.pickup||c.service_addresses.some(a=>a.trash_pickup_schedules.some(p=>String(p.weekday)===filters.pickup))));}
export async function crmCustomer(id:string){const safe=/^[0-9a-f-]{36}$/i.test(id)?id:"00000000-0000-0000-0000-000000000000";return (await databaseRequest<CustomerRow[]>(`customers?id=eq.${safe}&select=id,full_name,email,phone,account_status,service_addresses(id,line1,city,region,postal_code,preferred_return_location,access_instructions,gate_information,animal_warning,municipalities(name),trash_pickup_schedules(weekday,source,verification_status,cleaning_day_assignments(normal_weekday,cleaning_date))),subscriptions(payment_status,subscription_status,service_status,service_plan_versions(base_price_cents,service_plans(id,display_name)))`))[0]??null;}
export type VisitRow={id:string;status:string;scheduled_for:string|null;cleaning_confirmed:boolean;bins_returned:boolean;customers:{full_name:string};route_stops:{service_addresses:{line1:string;city:string;preferred_return_location:string|null;access_instructions:string|null;animal_warning:string|null;bins:{id:string;identifier:string|null;description:string|null}[]}}|null;visit_photographs:{id:string;kind:string}[]};
export async function visits(id?:string){return databaseRequest<VisitRow[]>(`service_visits?${id?`id=eq.${id}&`:""}select=id,status,scheduled_for,cleaning_confirmed,bins_returned,customers(full_name),route_stops(service_addresses(line1,city,preferred_return_location,access_instructions,animal_warning,bins(id,identifier,description))),visit_photographs(id,kind)&order=scheduled_for`);}
