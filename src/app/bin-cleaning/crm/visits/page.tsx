import { AppShell } from "@/components/bin-cleaning/AppShell";import { visits } from "@/lib/bin-cleaning/queries";export const dynamic="force-dynamic";
export default async function Visits(){const rows=await visits();return <AppShell area="Internal CRM"><h2 className="text-2xl font-black">Visits</h2><div className="mt-5 grid gap-4">{rows.map(v=><article className="card p-5" key={v.id}><div className="flex flex-wrap justify-between gap-2"><strong>{v.customers.full_name}</strong><span>{v.status}</span></div><p>{v.route_stops?.service_addresses.line1??"Unassigned address"}</p><p className="text-sm text-zinc-500">{v.scheduled_for??"Not scheduled"}</p></article>)}</div></AppShell>}
