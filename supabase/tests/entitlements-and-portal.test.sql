begin;create extension if not exists pgtap with schema extensions;select plan(8);set local role authenticated;select set_config('request.jwt.claim.sub','00000000-0000-4000-8000-000000000001',true);select set_config('request.jwt.claim.role','authenticated',true);
update bins set dirty_this_visit=false where service_address_id='30000000-0000-4000-8000-000000000001';update bins set dirty_this_visit=true where id=(select id from bins where service_address_id='30000000-0000-4000-8000-000000000001' limit 1);select is((select count(*) from bins where dirty_this_visit),1::bigint,'customer can select owned dirty bin');update bins set dirty_this_visit=false where service_address_id='30000000-0000-4000-8000-000000000001';select is((select count(*) from bins where dirty_this_visit),0::bigint,'omitted owned bin selection can be cleared');
update customer_contact_preferences set email_allowed=true where customer_id='20000000-0000-4000-8000-000000000001';select is((select email_allowed from customer_contact_preferences where customer_id='20000000-0000-4000-8000-000000000001'),true,'saved contact preference persists');
select set_config('request.jwt.claim.sub','00000000-0000-4000-8000-000000000010',true);select throws_like($$insert into cleaning_entitlements(customer_id,paid_service_cycle_id,idempotency_key) values('20000000-0000-4000-8000-000000000001','a0000000-0000-4000-8000-000000000001','duplicate-cycle')$$,'%duplicate key%','one entitlement per paid cycle');select throws_like($$insert into paid_service_cycles(customer_id,service_plan_version_id,cycle_start,idempotency_key) values('20000000-0000-4000-8000-000000000001',(select id from service_plan_versions where plan_id='monthly'),'2026-09-01','test-cycle-monthly-2026-08')$$,'%duplicate key%','paid-cycle idempotency prevents duplicates');
update service_visits set status='completed',completed_at=now() where id='80000000-0000-4000-8000-000000000001';set constraints enforce_visit_completion immediate;insert into service_visits(id,customer_id,assigned_technician_id,status,cleaning_confirmed,bins_returned,entitlement_id) values('80000000-0000-4000-8000-000000000099','20000000-0000-4000-8000-000000000001','00000000-0000-4000-8000-000000000012','after_photo_complete',true,true,'b0000000-0000-4000-8000-000000000001');insert into visit_photographs(service_visit_id,kind,storage_path,uploaded_by) values('80000000-0000-4000-8000-000000000099','before','test-only/duplicate-before','00000000-0000-4000-8000-000000000012'),('80000000-0000-4000-8000-000000000099','after','test-only/duplicate-after','00000000-0000-4000-8000-000000000012');select throws_like($$update service_visits set status='completed' where id='80000000-0000-4000-8000-000000000099'$$,'%duplicate key%','one completed cleaning per entitlement');
update service_plans set display_name=display_name where id='monthly';select is((select entity_key from audit_events where entity_table='service_plans' and entity_key='monthly' order by created_at desc limit 1),'monthly','audit records text primary key');select is((select entity_id from audit_events where entity_table='service_plans' and entity_key='monthly' order by created_at desc limit 1),null::uuid,'text primary key is not cast to uuid');select * from finish();rollback;
